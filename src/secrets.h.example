#pragma once

// Copy to secrets.h (which is gitignored) and fill in your networks.
// X(ssid, password) is expanded once per network by connect_wifi_async().
#define WIFI_AP_LIST(X)                     \
    X("my-ssid", "my-password")             \
    X("my-other-ssid", "my-other-password")
