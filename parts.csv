# Name,   Type, SubType,  Offset,   Size,     Flags
# Two app slots so OTA has somewhere to write. 1769472 B (0x1B0000) each; the image was 1395408 B
# when this table landed, so ~21% headroom -- check `pio run` output before assuming it still fits.
#
# nvs keeps its original offset and size ON PURPOSE: calibration lives there (settings.h goes through
# <EEPROM.h>, which on arduino-esp32 3.x is an NVS blob, not a partition of its own). Moving or
# erasing it silently destroys every bench calibration on the device, so reflash with
# `pio run -t upload`, never `esptool.py erase_flash`.
#
# spiffs shrank from 0x160000 to 0x80000 to make room. Nothing mounts it -- no SPIFFS/LittleFS
# anywhere in src/, no data/ dir, no uploadfs step -- it is kept only as reserve.
nvs,      data, nvs,      0x9000,   0x5000,
otadata,  data, ota,      0xe000,   0x2000,
app0,     app,  ota_0,    0x10000,  0x1B0000,
app1,     app,  ota_1,    0x1C0000, 0x1B0000,
spiffs,   data, spiffs,   0x370000, 0x80000,
coredump, data, coredump, 0x3F0000, 0x10000,
